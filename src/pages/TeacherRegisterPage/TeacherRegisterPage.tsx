import * as React from "react";

type TeacherRegisterPageProps = {
  //
};

const TeacherRegisterPage: React.FC<any> = () => {
  return <div>TeacherRegisterPage</div>;
};

export default TeacherRegisterPage;
